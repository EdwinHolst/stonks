_Raw financial data for Merlin._

Default format: **CSV** (one file per statement, e.g. `income.csv`,
`balance-sheet.csv`, `cash-flow.csv`). Other formats (JSON, Markdown) are
allowed when they fit better — keep tooling format-agnostic.
